schema_version = "1.3"

artifacts = ["/app/report.json"]

[task]
name = "dynamo/log-report"
description = "Parse an Apache-style access log into a small JSON summary report."
authors = []
keywords = []

[metadata]
category = "data_processing_and_etl"
subcategory = "text_processing"
task_objective = ["transform", "generate"]
artifact_type = ["generated_output_artifact"]
expert_time_estimate_hours = 0.3
model_tested = "GPT-5.4"
agent_tested = "Terminus-2"
difficulty_explanation = "Parse a small access log and emit summary statistics."
solution_explanation = "Read the access log and generate a JSON summary report."
verification_explanation = "Validate the generated JSON report contents."

[verifier]
timeout_sec = 120.0
collect = []

[agent]
timeout_sec = 120.0

[environment]
network_mode = "public"
build_timeout_sec = 600.0
os = "linux"
cpus = 1
memory_mb = 2048
storage_mb = 10240
gpus = 0
allow_internet = true
mcp_servers = []

[solution]
command = "solution/solve.sh"

